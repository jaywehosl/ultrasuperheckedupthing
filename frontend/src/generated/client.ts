// Code generated by scripts/build-api-client.mjs. DO NOT EDIT.
// Source of truth: src/pages/api-docs/endpoints.ts
//
// One typed function per backend endpoint, grouped by domain. Path params are
// positional args; query/body params are a single typed object. Every call
// returns the standard `Msg<T>` envelope. Response payloads are `unknown` by
// design — validate them at the call site with the matching zod schema in
// src/schemas/. Pass a type argument to narrow: `clientsApi.list<ClientRecord[]>()`.
/* eslint-disable @typescript-eslint/no-explicit-any */
import { HttpUtil, type Msg, type HttpOptions } from '@/utils';

const JSON_HEADERS: HttpOptions = { headers: { 'Content-Type': 'application/json' } };

function enc(v: string | number): string {
  return encodeURIComponent(String(v));
}

/** Authentication */
export const authenticationApi = {
  /** Authenticate with username + password and receive a session cookie. Required before any cookie-based API call. */
  login<T = unknown>(body: { username: string; password: string; twoFactorCode: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/login', body, options);
  },

  /** Clear the session cookie. Requires the CSRF header for browser sessions. */
  logout<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/logout', body, options);
  },

  /** Mint a CSRF token for the current session. The SPA replays it in the X-CSRF-Token header on unsafe requests. Bearer-token callers can skip this — the middleware short-circuits CSRF for authenticated API requests. */
  csrfToken<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/csrf-token', params, options);
  },

  /** Returns whether 2FA is enabled on the panel — used by the login page to decide whether to show the OTP field. */
  getTwoFactorEnable<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/getTwoFactorEnable', body, options);
  },
};

/** Inbounds */
export const inboundsApi = {
  /** List every inbound owned by the authenticated user, including each inbound’s clientStats traffic counters. settings, streamSettings, and sniffing are returned as nested JSON objects (no escaped strings); legacy callers that send them back a */
  list<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/inbounds/list', params, options);
  },

  /** Same shape as /list but with settings.clients[] stripped down to {email, enable, comment} and ClientStats not enriched with UUID/SubId. Use this for list pages; fetch /get/:id when you need the full per-client payload (uuid, password, flow, */
  listSlim<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/inbounds/list/slim', params, options);
  },

  /** Lightweight picker projection of the authenticated user’s inbounds. Returns only id, remark, protocol, port, and a server-computed tlsFlowCapable flag (true for VLESS / port-fallback on TCP with tls or reality). Use this for dropdowns and a */
  options<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/inbounds/options', params, options);
  },

  /** Fetch a single inbound by numeric ID. */
  getById<T = unknown>(id: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/inbounds/get/${enc(id)}`, params, options);
  },

  /** Create a new inbound. Send the full inbound payload (protocol, port, settings, streamSettings, sniffing, remark, expiryTime, total, enable). settings, streamSettings, and sniffing may be sent as nested JSON objects (preferred) or as JSON-en */
  add<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/inbounds/add', body, { ...JSON_HEADERS, ...options });
  },

  /** Delete an inbound by ID. Also removes its associated client stats rows. */
  delById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/del/${enc(id)}`, body, options);
  },

  /** Delete many inbounds in one call. Processes the list sequentially; failures are reported per id and the rest still proceed. Restarts xray at most once. */
  bulkDel<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/inbounds/bulkDel', body, { ...JSON_HEADERS, ...options });
  },

  /** Replace an inbound’s configuration. Body shape mirrors /add. Heavy on inbounds with thousands of clients — prefer /setEnable for enable-only flips. */
  updateById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/update/${enc(id)}`, body, options);
  },

  /** Toggle only the enable flag without serialising the whole settings JSON. Recommended for UI switches on large inbounds. */
  setEnableById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/setEnable/${enc(id)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Zero out upload + download counters for a single inbound. Does not touch per-client counters. */
  resetTrafficById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/${enc(id)}/resetTraffic`, body, options);
  },

  /** Remove every client attached to a single inbound while keeping the inbound itself. Collects emails from settings.clients[] and feeds them into the optimized bulk-delete path (runtime user removal + traffic-row cleanup + SyncInbound). Destru */
  delAllClientsById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/${enc(id)}/delAllClients`, body, options);
  },

  /** Reset upload + download counters on every inbound. Destructive — accounting history is lost. */
  resetAllTraffics<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/inbounds/resetAllTraffics', body, options);
  },

  /** Bulk-import an inbound from a JSON blob (e.g. one exported via the UI). The body uses form encoding with a single "data" field. */
  import<T = unknown>(body: { data: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/inbounds/import', body, options);
  },

  /** List the fallback rules attached to a master VLESS/Trojan TCP-TLS inbound. Each rule links one child inbound (the dest) to optional SNI/ALPN/path/dest/xver match criteria. When dest is empty the child inbound's listen+port is used. */
  fallbacksById<T = unknown>(id: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/inbounds/${enc(id)}/fallbacks`, params, options);
  },

  /** Replace the entire fallback list for a master inbound. Body is JSON. Triggers an Xray restart. */
  postFallbacksById<T = unknown>(id: string | number, body: { fallbacks: unknown[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/inbounds/${enc(id)}/fallbacks`, body, { ...JSON_HEADERS, ...options });
  },
};

/** Server */
export const serverApi = {
  /** Real-time machine snapshot: CPU, memory, swap, disk, network IO, load averages, open connections, Xray state. Cached and refreshed every 2 seconds in the background. */
  status<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/status', params, options);
  },

  /** Legacy: aggregated CPU history. Use /history/cpu/:bucket instead — same data with a uniform {t, v} shape. */
  cpuHistoryByBucket<T = unknown>(bucket: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/server/cpuHistory/${enc(bucket)}`, params, options);
  },

  /** Aggregated time-series for one metric. Returns an array of {t, v} samples covering the last ~6 hours. */
  historyByMetricByBucket<T = unknown>(metric: string | number, bucket: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/server/history/${enc(metric)}/${enc(bucket)}`, params, options);
  },

  /** Xray runtime metrics state — whether the xray config has a `metrics` block, which expvar keys are flowing, and the current snapshot values for each. Returns an empty state when metrics are not configured. */
  xrayMetricsState<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/xrayMetricsState', params, options);
  },

  /** Time-series history for one Xray runtime metric over the last ~6 hours. Same {t, v} shape as /history/:metric/:bucket. */
  xrayMetricsHistoryByMetricByBucket<T = unknown>(metric: string | number, bucket: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/server/xrayMetricsHistory/${enc(metric)}/${enc(bucket)}`, params, options);
  },

  /** Latest snapshot from the Xray observatory — per-outbound latency, health status, and last-probe time. Only populated when the Xray config has an observatory configured. */
  xrayObservatory<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/xrayObservatory', params, options);
  },

  /** Time-series of observatory probe results for one outbound tag. Same {t, v} shape as the other history endpoints. */
  xrayObservatoryHistoryByTagByBucket<T = unknown>(tag: string | number, bucket: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/server/xrayObservatoryHistory/${enc(tag)}/${enc(bucket)}`, params, options);
  },

  /** List Xray binary versions available for install on this host. */
  getXrayVersion<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getXrayVersion', params, options);
  },

  /** Check whether a newer 3x-ui release is available on GitHub. */
  getPanelUpdateInfo<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getPanelUpdateInfo', params, options);
  },

  /** Return the assembled Xray config that’s currently running on this host. */
  getConfigJson<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getConfigJson', params, options);
  },

  /** Stream the SQLite database file as an attachment. Use as a manual backup. */
  getDb<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getDb', params, options);
  },

  /** Stream a cross-engine migration file as an attachment: a .dump (SQL text) on SQLite, or a .db SQLite database built from the live data on PostgreSQL. */
  getMigration<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getMigration', params, options);
  },

  /** Generate a fresh UUID v4. Convenience helper for client IDs. */
  getNewUUID<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getNewUUID', params, options);
  },

  /** Return this panel's own web TLS certificate and key file paths. The central panel calls it on a node (via the node API token) so "Set Cert from Panel" fills a node-assigned inbound with paths that exist on the node. */
  getWebCertFiles<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getWebCertFiles', params, options);
  },

  /** Generate a new X25519 keypair for Reality. */
  getNewX25519Cert<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getNewX25519Cert', params, options);
  },

  /** Generate a new ML-DSA-65 keypair (post-quantum signature). Returns {privateKey, publicKey, seed}. */
  getNewmldsa65<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getNewmldsa65', params, options);
  },

  /** Generate a new ML-KEM-768 keypair (post-quantum KEM). Returns {clientKey, serverKey}. */
  getNewmlkem768<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getNewmlkem768', params, options);
  },

  /** Generate VLESS encryption auth options. Returns an auths array each with id, label, encryption, and decryption fields. */
  getNewVlessEnc<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/server/getNewVlessEnc', params, options);
  },

  /** Stop the Xray binary. All proxies go offline immediately. */
  stopXrayService<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/server/stopXrayService', body, options);
  },

  /** Reload Xray with the current config. Typically required after structural inbound or routing changes. */
  restartXrayService<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/server/restartXrayService', body, options);
  },

  /** Download and install the specified Xray version. Pass "latest" for the newest release. */
  installXrayByVersion<T = unknown>(version: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/server/installXray/${enc(version)}`, body, options);
  },

  /** Self-update the panel to the latest version. The server restarts on success. */
  updatePanel<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/server/updatePanel', body, options);
  },

  /** Refresh the default GeoIP / GeoSite data files. Body can include a fileName, or use the /:fileName variant. */
  updateGeofile<T = unknown>(body: { fileName: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/server/updateGeofile', body, options);
  },

  /** Refresh a single Geo file by filename (e.g. geoip.dat, geosite.dat). */
  updateGeofileByFileName<T = unknown>(fileName: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/server/updateGeofile/${enc(fileName)}`, body, options);
  },

  /** Return the last N lines of the panel’s own log. */
  logsByCount<T = unknown>(count: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/server/logs/${enc(count)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Return the last N lines of the Xray process log. */
  xraylogsByCount<T = unknown>(count: string | number, body: { filter: string; showDirect: string; showBlocked: string; showProxy: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/server/xraylogs/${enc(count)}`, body, options);
  },

  /** Restore the panel DB from an uploaded SQLite file (multipart form, field name "db"). The panel restarts after restore. Destructive. */
  importDB<T = unknown>(form: FormData, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/server/importDB', form, options);
  },

  /** Generate a new ECH (Encrypted Client Hello) keypair and config list for the given SNI. */
  getNewEchCert<T = unknown>(body: { sni: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/server/getNewEchCert', body, options);
  },
};

/** Clients */
export const clientsApi = {
  /** List every client with its attached inbound IDs and traffic record. The reverse field, if set, is returned as a nested JSON object (legacy JSON-encoded-string form is still accepted on write). */
  list<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/clients/list', params, options);
  },

  /** Filter, sort, and paginate clients on the server. Each item is a slim row (no uuid/password/auth/flow/security/reverse/tgId) so the clients page can ship 25-ish rows in a few KB instead of the full table. The response also includes a summar */
  listPaged<T = unknown>(params: { page: number; pageSize: number; search: string; filter: string; protocol: string; sort: string; order: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/clients/list/paged', params, options);
  },

  /** Fetch one client by email, including the inbound IDs it is attached to. */
  getByEmail<T = unknown>(email: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/clients/get/${enc(email)}`, params, options);
  },

  /** Create a new client and attach it to one or more inbounds in a single call. Body is JSON. Per-protocol secrets (UUID for VLESS/VMess, password for Trojan/Shadowsocks, auth for Hysteria) are generated server-side when omitted, so callers can */
  add<T = unknown>(body: { client: Record<string, unknown>; inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/add', body, { ...JSON_HEADERS, ...options });
  },

  /** Update an existing client by email. Changes propagate to every attached inbound. Body is the JSON client payload — supply the full set of fields you want to keep (the server replaces the row, it does not patch). */
  updateByEmail<T = unknown>(email: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/update/${enc(email)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Delete a client by email. Removes it from every attached inbound and drops its traffic record unless keepTraffic=1 is passed. */
  delByEmail<T = unknown>(email: string | number, body: unknown, params: { keepTraffic: number }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/del/${enc(email)}`, body, { params: params, ...options });
  },

  /** Attach an existing client to one or more additional inbounds. Body is JSON. */
  attachByEmail<T = unknown>(email: string | number, body: { inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/${enc(email)}/attach`, body, { ...JSON_HEADERS, ...options });
  },

  /** Detach a client from one or more inbounds without deleting the client. */
  detachByEmail<T = unknown>(email: string | number, body: { inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/${enc(email)}/detach`, body, { ...JSON_HEADERS, ...options });
  },

  /** Reset the up/down counters for every client globally. Quotas and expiry are not affected. Triggers an Xray restart if any counter actually moved. */
  resetAllTraffics<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/resetAllTraffics', body, options);
  },

  /** Delete every client whose traffic quota is exhausted (used >= total, when reset is disabled) or whose expiry has passed. Returns the deleted count and triggers an Xray restart when any client was on a running inbound. */
  delDepleted<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/delDepleted', body, options);
  },

  /** Shift expiry and/or traffic quota for many clients in one call. addDays/addBytes may be negative. Clients with unlimited expiry (expiryTime=0) or unlimited traffic (totalGB=0) are skipped for the corresponding field — bulk extend never conv */
  bulkAdjust<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkAdjust', body, { ...JSON_HEADERS, ...options });
  },

  /** Delete many clients in one call. The server processes the list sequentially so each delete sees the committed state of the previous one — avoids the race the per-email fan-out had on the panel side. Pass keepTraffic=true to retain the xray_ */
  bulkDel<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkDel', body, { ...JSON_HEADERS, ...options });
  },

  /** Create many clients in one call. Body is a JSON array of {client, inboundIds} payloads — the same shape /add accepts. Items are processed sequentially; per-email skip reasons are returned for items that fail (e.g., duplicate email). Trigger */
  bulkCreate<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkCreate', body, { ...JSON_HEADERS, ...options });
  },

  /** Add many clients to a group in one call. Updates clients.group_name and patches the matching client entry inside every owning inbound's settings JSON in a single transaction. If the group name does not yet exist (in client_groups or as a de */
  groupsBulkAdd<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/bulkAdd', body, { ...JSON_HEADERS, ...options });
  },

  /** Clear the group label on many clients in one call. Inverse of /groups/bulkAdd. Clients themselves are kept — only the group label is cleared from clients.group_name and from each owning inbound's settings JSON. Groups become empty if all th */
  groupsBulkRemove<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/bulkRemove', body, { ...JSON_HEADERS, ...options });
  },

  /** Attach many existing clients to many inbounds in one call. Each client keeps its identity (email/UUID/password/subId) and a shared traffic row; all clients are added to a target inbound in a single AddInboundClient call. Clients already pre */
  bulkAttach<T = unknown>(body: { emails: unknown[]; inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkAttach', body, { ...JSON_HEADERS, ...options });
  },

  /** Mirror of bulkAttach: detach many existing clients from many inbounds in one call. For each email, intersects the client's current inbounds with the requested set and detaches from those only; (email, inbound) pairs where the client is not  */
  bulkDetach<T = unknown>(body: { emails: unknown[]; inboundIds: number[] }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkDetach', body, { ...JSON_HEADERS, ...options });
  },

  /** Zero up/down counters for many clients in one call. Loops the single-reset path so each client is re-enabled across its attached inbounds and pushed to Xray/remote nodes. Returns the count of successfully reset clients. */
  bulkResetTraffic<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/bulkResetTraffic', body, { ...JSON_HEADERS, ...options });
  },

  /** List all client groups with their member counts. Merges persisted groups (rows in client_groups, including empty placeholders) with the distinct group_name values currently set on clients. Sorted alphabetically (case-insensitive). */
  groups<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/clients/groups', params, options);
  },

  /** Return just the email list of clients that currently belong to the given group. Useful for fanning a single bulk action over an entire group without round-tripping the full client list. */
  groupsEmailsByName<T = unknown>(name: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/clients/groups/${enc(name)}/emails`, params, options);
  },

  /** Create a new empty (placeholder) group. The group becomes selectable in client forms and the filter drawer even before any client is added to it. Errors if a group with the same name already exists. */
  groupsCreate<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/create', body, { ...JSON_HEADERS, ...options });
  },

  /** Rename a group. The new name is applied to the client_groups row AND propagated to every matching client (both clients.group_name and the client entry inside every owning inbound's settings JSON) in a single transaction. Returns the number  */
  groupsRename<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/rename', body, { ...JSON_HEADERS, ...options });
  },

  /** Remove a group. Deletes the client_groups row and clears the group label from every matching client (both clients.group_name and the inbound settings JSON). The clients themselves are NOT deleted — use /bulkDel after filtering by group for  */
  groupsDelete<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/groups/delete', body, { ...JSON_HEADERS, ...options });
  },

  /** Zero out a single client’s up/down counters. Re-enables the client across every attached inbound and pushes the change to Xray (or the remote node) so depleted users can connect again immediately. */
  resetTrafficByEmail<T = unknown>(email: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/resetTraffic/${enc(email)}`, body, options);
  },

  /** Manually adjust a client’s upload + download counters. Useful for migrations from external accounting systems. */
  updateTrafficByEmail<T = unknown>(email: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/updateTraffic/${enc(email)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** List source IPs that have connected with the given client’s credentials. Returns an array of "ip (timestamp)" strings. */
  ipsByEmail<T = unknown>(email: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/ips/${enc(email)}`, body, options);
  },

  /** Reset the recorded IP list for a client. */
  clearIpsByEmail<T = unknown>(email: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/clients/clearIps/${enc(email)}`, body, options);
  },

  /** List the emails of currently connected clients (last seen within the heartbeat window), deduped across every node. */
  onlines<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/onlines', body, options);
  },

  /** Online client emails grouped by the node that reported them. The local panel uses key "0"; each remote node uses its node id. Lets the inbounds page show online status per node instead of merging every node together. */
  onlinesByNode<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/onlinesByNode', body, options);
  },

  /** Inbound tags that carried traffic within the heartbeat window, grouped by node (local panel uses key "0"). Pairs with onlinesByNode so the inbounds page only marks a multi-inbound client online on the inbounds it actually used. Nodes that d */
  activeInbounds<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/activeInbounds', body, options);
  },

  /** Map of client email → last-seen unix timestamp. */
  lastOnline<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/clients/lastOnline', body, options);
  },

  /** Traffic counters for a client identified by email. */
  trafficByEmail<T = unknown>(email: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/clients/traffic/${enc(email)}`, params, options);
  },

  /** Return every protocol URL (vless://, vmess://, trojan://, ss://, hysteria://, hy2://) for clients matching the subscription ID. Same result set as /sub/<subId>, but as a JSON array — no base64. When an inbound has streamSettings.externalPro */
  subLinksBySubId<T = unknown>(subId: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/clients/subLinks/${enc(subId)}`, params, options);
  },

  /** Return every URL for one client across all attached inbounds — the same strings the Copy URL button copies in the panel UI. Supported protocols: vmess, vless, trojan, shadowsocks, hysteria. If streamSettings.externalProxy is set, returns on */
  linksByEmail<T = unknown>(email: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/clients/links/${enc(email)}`, params, options);
  },
};

/** Nodes */
export const nodesApi = {
  /** List every configured node with its connection details, health, and last heartbeat patch. */
  list<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/nodes/list', params, options);
  },

  /** Fetch a single node by ID. */
  getById<T = unknown>(id: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/nodes/get/${enc(id)}`, params, options);
  },

  /** Fetch a node's own web TLS certificate/key file paths (proxied to the node). Used by the inbound form's "Set Cert from Panel" so a node-assigned inbound gets paths that exist on the node, not the central panel. */
  webCertById<T = unknown>(id: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/nodes/webCert/${enc(id)}`, params, options);
  },

  /** Register a new remote node. Provide its URL, apiToken, and optional remark / allowPrivateAddress flag. */
  add<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/nodes/add', body, { ...JSON_HEADERS, ...options });
  },

  /** Replace a node’s connection details. Same body shape as /add. */
  updateById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/update/${enc(id)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Delete a node. Inbounds bound to it are not auto-migrated. */
  delById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/del/${enc(id)}`, body, options);
  },

  /** Pause or resume traffic sync with this node. */
  setEnableById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/setEnable/${enc(id)}`, body, { ...JSON_HEADERS, ...options });
  },

  /** Probe a node without saving it. Uses the body as connection details and returns the same heartbeat snapshot a registered node would have. */
  test<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/nodes/test', body, { ...JSON_HEADERS, ...options });
  },

  /** Connect to the node over HTTPS without verifying its certificate and return the leaf certificate's SHA-256 (base64). Used by the Add/Edit Node dialog to fetch and pin a self-signed certificate. Uses the same body as /test. */
  certFingerprint<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/nodes/certFingerprint', body, { ...JSON_HEADERS, ...options });
  },

  /** Probe an existing node, updating its cached health state. */
  probeById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/nodes/probe/${enc(id)}`, body, options);
  },

  /** Trigger the official panel self-updater on each given node (downloads the latest release and restarts). Only enabled, online nodes are updated; offline/disabled ones are reported as skipped. Returns a per-node result list. */
  updatePanel<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/nodes/updatePanel', body, { ...JSON_HEADERS, ...options });
  },

  /** Aggregated metric history for a node — same shape as /server/history, scoped to one node. */
  historyByIdByMetricByBucket<T = unknown>(id: string | number, metric: string | number, bucket: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/panel/api/nodes/history/${enc(id)}/${enc(metric)}/${enc(bucket)}`, params, options);
  },
};

/** Custom Geo */
export const customGeoApi = {
  /** List configured custom geo sources with their type, alias, URL, status, and last-download timestamp. */
  list<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/custom-geo/list', params, options);
  },

  /** List geo aliases currently usable in routing rules — both built-in defaults and the user-configured ones. */
  aliases<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/api/custom-geo/aliases', params, options);
  },

  /** Register a custom geo source. Alias is auto-normalised; URL must point to a .dat / .json blob. */
  add<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/custom-geo/add', body, { ...JSON_HEADERS, ...options });
  },

  /** Replace a custom geo source. Same body shape as /add. */
  updateById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/custom-geo/update/${enc(id)}`, body, options);
  },

  /** Remove a custom geo source and its cached file. */
  deleteById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/custom-geo/delete/${enc(id)}`, body, options);
  },

  /** Re-download one custom geo source on demand. */
  downloadById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/api/custom-geo/download/${enc(id)}`, body, options);
  },

  /** Re-download every configured custom geo source. Errors are reported per-source in the response. */
  updateAll<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/custom-geo/update-all', body, options);
  },
};

/** Backup */
export const backupApi = {
  /** Send a fresh DB backup to every Telegram chat configured as an admin recipient. No body, no params. */
  backuptotgbot<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/api/backuptotgbot', body, options);
  },
};

/** Settings */
export const settingsApi = {
  /** Return every panel setting: web server, Telegram bot, subscription, security, LDAP. The full JSON blob that the Settings page edits. */
  panelSettingAll<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/all', body, options);
  },

  /** Return the computed default settings based on the request host. Useful to preview what a fresh install would use. */
  panelSettingDefaultSettings<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/defaultSettings', body, options);
  },

  /** Persist every setting at once. The body mirrors the shape returned by /all. Invalid values (bad ports, missing cert pairs, etc.) are rejected before write. */
  panelSettingUpdate<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/update', body, { ...JSON_HEADERS, ...options });
  },

  /** Change the panel admin username and password. Requires the current credentials for verification. The session is refreshed with the new values on success. */
  panelSettingUpdateUser<T = unknown>(body: { oldUsername: string; oldPassword: string; newUsername: string; newPassword: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/updateUser', body, options);
  },

  /** Restart the entire 3x-ui process after a 3-second grace period. The connection drops immediately; the panel comes back online ~5-10 seconds later. */
  panelSettingRestartPanel<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/restartPanel', body, options);
  },

  /** Return the built-in default Xray JSON config template that ships with this panel version. */
  panelSettingGetDefaultJsonConfig<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/setting/getDefaultJsonConfig', params, options);
  },
};

/** API Tokens */
export const apiTokensApi = {
  /** List every API token, enabled or not. The token value is never returned — only metadata. */
  panelSettingApiTokens<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/setting/apiTokens', params, options);
  },

  /** Mint a new API token. Name must be unique and 1-64 characters; the token string is server-generated and returned only in this response — it is stored hashed and cannot be retrieved later. */
  panelSettingApiTokensCreate<T = unknown>(body: { name: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/setting/apiTokens/create', body, options);
  },

  /** Permanently delete a token. Any caller using it stops authenticating immediately. */
  panelSettingApiTokensDeleteById<T = unknown>(id: string | number, body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/setting/apiTokens/delete/${enc(id)}`, body, options);
  },

  /** Toggle a token enabled/disabled without deleting it. Disabled tokens are rejected by checkAPIAuth on the next request. */
  panelSettingApiTokensSetEnabledById<T = unknown>(id: string | number, body: { enabled: boolean }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/setting/apiTokens/setEnabled/${enc(id)}`, body, options);
  },
};

/** Xray Settings */
export const xraySettingsApi = {
  /** Return the Xray config template (JSON string), available inbound tags, client reverse tags, and the configured outbound test URL in one response. */
  panelXray<T = unknown>(body?: unknown, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/xray/', body, options);
  },

  /** Return the built-in default Xray config shipped with the panel (identical to /panel/setting/getDefaultJsonConfig). */
  panelXrayGetDefaultJsonConfig<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/xray/getDefaultJsonConfig', params, options);
  },

  /** Return traffic statistics for every outbound. Each outbound shows up/down/total counters. */
  panelXrayGetOutboundsTraffic<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/xray/getOutboundsTraffic', params, options);
  },

  /** Return the most recent Xray process stdout/stderr output. Useful to check for startup errors or runtime warnings. */
  panelXrayGetXrayResult<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/panel/xray/getXrayResult', params, options);
  },

  /** Save the Xray JSON config template and optionally the outbound test URL. Both are sent as form fields. */
  panelXrayUpdate<T = unknown>(body: { xraySetting: string; outboundTestUrl: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/xray/update', body, options);
  },

  /** Manage Cloudflare Warp integration. The action parameter selects the operation. */
  panelXrayWarpByAction<T = unknown>(action: string | number, body: { privateKey: string; publicKey: string; license: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/xray/warp/${enc(action)}`, body, options);
  },

  /** Manage NordVPN integration. The action parameter selects the operation. */
  panelXrayNordByAction<T = unknown>(action: string | number, body: { countryId: string; token: string; key: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>(`/panel/xray/nord/${enc(action)}`, body, options);
  },

  /** Reset traffic counters for a specific outbound by tag. */
  panelXrayResetOutboundsTraffic<T = unknown>(body: { tag: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/xray/resetOutboundsTraffic', body, options);
  },

  /** Test an outbound configuration. Sends the outbound JSON (required), optionally all outbounds (to resolve sockopt.dialerProxy dependencies), and a mode flag. */
  panelXrayTestOutbound<T = unknown>(body: { outbound: string; allOutbounds: string; mode: string }, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.post<T>('/panel/xray/testOutbound', body, options);
  },
};

/** Subscription Server */
export const subscriptionApi = {
  /** Return base64-encoded subscription links for all enabled clients matching the subscription ID. When the request has an Accept: text/html header or ?html=1, renders a styled info page instead. Default path: /sub/:subid. */
  subPathSubid<T = unknown>(subPath: string | number, subid: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/${enc(subPath)}${enc(subid)}`, params, options);
  },

  /** Return subscription as a JSON array of proxy configs (one per enabled client). Only when JSON subscription is enabled in settings. Default path: /json/:subid. */
  jsonPathSubid<T = unknown>(jsonPath: string | number, subid: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/${enc(jsonPath)}${enc(subid)}`, params, options);
  },

  /** Return subscription as a Clash/Mihomo-compatible YAML config, including configured global Clash routing rules. Only when Clash subscription is enabled in settings. Default path: /clash/:subid. */
  clashPathSubid<T = unknown>(clashPath: string | number, subid: string | number, params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>(`/${enc(clashPath)}${enc(subid)}`, params, options);
  },
};

/** WebSocket */
export const websocketApi = {
  /** Upgrade an HTTP connection to a WebSocket. Requires an authenticated session cookie (Bearer token auth is not supported here). Returns 101 Switching Protocols on success. The server then pushes JSON messages described below. */
  ws<T = unknown>(params?: Record<string, unknown>, options?: HttpOptions): Promise<Msg<T>> {
    return HttpUtil.get<T>('/ws', params, options);
  },
};
